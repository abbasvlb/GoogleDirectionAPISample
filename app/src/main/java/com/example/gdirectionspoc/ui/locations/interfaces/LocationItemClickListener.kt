package com.example.gdirectionspoc.ui.locations.interfaces

interface LocationItemClickListener {

    fun onItemClicked(id: Int)

    fun onEditClicked(id : Int)

    fun onGetLocationClicked(id: Int)
}